en:
  loading_station_uncertainty: "Loading WLP uncertainties by station from %{json_file}."
  loading_datalogger_uncertainty: "Loading uncertainty constants by DataLoggerType from %{json_file}."
  constant_found_in_json: "%{constant_key} for '%{datalogger_type}' found in JSON: %{constant}."
  datalogger_not_in_json: "DataLoggerType '%{datalogger_type}' not in JSON (%{constant_key}) — using default value: %{default}."
  loading_ssp_errors: "Loading SSP errors from %{file_path}."
  azimuthal_projection: "Azimuthal equidistant projection centered at (%{lat}, %{lon})."
  reprojecting_to_crs: "Reprojecting data to CRS: %{crs}."
  filtering_by_bbox: "Filtering data by bounding box (%{bbox}) with a buffer of %{buffer} meters."
  spatial_join_column: "Spatial join of column '%{column}' with a maximum distance of %{max_distance} meters."
  joining_ssp_errors: "Spatial join of depth data with SSP errors."
  tvu_mode_constant: "fixed constant"
  tvu_mode_mapping: "station mapping"
  tvu_mode: "TVU — mode: %{mode} (apply_water_level=%{apply_water_level}, already_at_chart_datum=%{already_at_chart_datum})."
  tvu_constant_resolved: "TVU — constant resolved from ProcessingContext (datalogger_type=%{datalogger_type}): %{constant_tvu}."
  computing_tvu: "Computing vertical uncertainty for depth data."
  tvu_depth_component: "TVU — depth_component: min=%{dc_min}, max=%{dc_max}, mean=%{dc_mean} (depth_coefficient_tvu=%{depth_coeff}, ssp_coeff min=%{ssp_min} / max=%{ssp_max})."
  tvu_station_component_constant: "TVU — station_component: fixed constant = %{constant}."
  tvu_station_component_mapping: "TVU — station_component: station mapping — min=%{sc_min}, max=%{sc_max}, mean=%{sc_mean} (wlo=%{wlo}, default wlp=%{wlp})."
  computing_thu: "Computing horizontal uncertainty for depth data."
  thu_constant_resolved: "THU — constant_thu resolved from ProcessingContext (datalogger_type=%{datalogger_type}): %{constant_thu} (TOML default = %{toml_default})."
  thu_constant_toml: "THU — constant_thu from TOML (no ProcessingContext): %{constant_thu}."
  thu_cone_angle: "THU — cone_angle_sonar=%{cone_angle}deg -> thu_depth_coeficient=%{depth_coeff}."
  thu_result: "THU — result: min=%{thu_min}, max=%{thu_max}, mean=%{thu_mean}."
