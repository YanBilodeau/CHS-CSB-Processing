fr:
  loading_station_uncertainty: "Chargement des incertitudes de WLP par station depuis %{json_file}."
  loading_datalogger_uncertainty: "Chargement des constantes d'incertitude par DataLoggerType depuis %{json_file}."
  constant_found_in_json: "%{constant_key} pour '%{datalogger_type}' trouvée dans le JSON : %{constant}."
  datalogger_not_in_json: "DataLoggerType '%{datalogger_type}' absent du JSON (%{constant_key}) — utilisation de la valeur par défaut : %{default}."
  loading_ssp_errors: "Chargement des erreurs SSP depuis %{file_path}."
  azimuthal_projection: "Projection équidistante azimutale centrée sur (%{lat}, %{lon})."
  reprojecting_to_crs: "Reprojection des données vers le CRS: %{crs}."
  filtering_by_bbox: "Filtrage données par bounding box (%{bbox}) avec un buffer de %{buffer} mètres."
  spatial_join_column: "Jointure spatiale de la colonne '%{column}' avec une distance maximale de %{max_distance} mètres."
  joining_ssp_errors: "Jointure spatiale des données de profondeur avec les erreurs SSP."
  tvu_mode_constant: "constante fixe"
  tvu_mode_mapping: "mapping par station"
  tvu_mode: "TVU — mode : %{mode} (apply_water_level=%{apply_water_level}, already_at_chart_datum=%{already_at_chart_datum})."
  tvu_constant_resolved: "TVU — constante résolue depuis ProcessingContext (datalogger_type=%{datalogger_type}) : %{constant_tvu}."
  computing_tvu: "Calcul de l'incertitude verticale des données de profondeur."
  tvu_depth_component: "TVU — depth_component : min=%{dc_min}, max=%{dc_max}, mean=%{dc_mean} (depth_coefficient_tvu=%{depth_coeff}, ssp_coeff min=%{ssp_min} / max=%{ssp_max})."
  tvu_station_component_constant: "TVU — station_component : constante fixe = %{constant}."
  tvu_station_component_mapping: "TVU — station_component : mapping par station — min=%{sc_min}, max=%{sc_max}, mean=%{sc_mean} (wlo=%{wlo}, défaut wlp=%{wlp})."
  computing_thu: "Calcul de l'incertitude horizontale des données de profondeur."
  thu_constant_resolved: "THU — constant_thu résolu depuis ProcessingContext (datalogger_type=%{datalogger_type}) : %{constant_thu} (TOML défaut = %{toml_default})."
  thu_constant_toml: "THU — constant_thu depuis TOML (pas de ProcessingContext) : %{constant_thu}."
  thu_cone_angle: "THU — cone_angle_sonar=%{cone_angle}° → thu_depth_coeficient=%{depth_coeff}."
  thu_result: "THU — résultat : min=%{thu_min}, max=%{thu_max}, mean=%{thu_mean}."
